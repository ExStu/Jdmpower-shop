import { IProduct } from '@/types/product.interface'
import { FC } from 'react'

const ProductPreview: FC<{product: IProduct}> = ({
  product
}) => {
  return (
    <div></div>
  )
}

export default ProductPreview;
